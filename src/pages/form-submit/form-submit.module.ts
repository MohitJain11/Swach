import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { FormSubmitPage } from './form-submit';

@NgModule({
  declarations: [
    // FormSubmitPage,
  ],
  imports: [
    IonicPageModule.forChild(FormSubmitPage),
  ],
})
export class FormSubmitPageModule {}
